language: node_js
dist: trusty
cache: yarn
sudo: required

node_js:
    - '7'

before_install:
    - 'curl -o- -L https://yarnpkg.com/install.sh | bash -s -- --version 1.0.2'
    - export PATH="$HOME/.yarn/bin:$PATH"

before_script:
    - 'yarn run build'

after_success:
    - 'yarn run coverage'
